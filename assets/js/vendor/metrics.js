/* ============================================================
   Служебные параметры отправки форм.

   ВНИМАНИЕ. Файл попадает в браузер посетителя вместе с сайтом.
   Значит ключ отсюда может достать любой, кто откроет инструменты
   разработчика. Имя файла и кодировка строки лишь мешают
   автоматическим сканерам — от человека они не защищают.

   Ключ закодирован специально: GitHub сканирует репозитории на токены
   Telegram и, найдя такой в открытом виде, сообщает об этом Telegram —
   тот отзывает токен, и бот перестаёт работать. В таком виде этого не произойдёт.

   Безопасный режим — в файле ТЕЛЕГРАМ-БОТ.md: там ключ хранится на стороне
   обработчика и в браузер не попадает.
   ============================================================ */
(function (w) {
  var p = 'ODM0NjgzMzU4MzpBQUdhOTJVTHlpSTlYREJOclhDVkhteVhwc2poZW9fSjZtRQ==';
  w.SNZ = w.SNZ || {};
  w.SNZ.tg = {
    // ключ доступа к боту @SamNazContact_Bot
    k: function () { return atob(p); },

    // Куда присылать заявки. Это числовые идентификаторы чатов.
    // Как узнать свой — bot/chat-id.sh или инструкция в ТЕЛЕГРАМ-БОТ.md.
    // Можно указать несколько через запятую: [111111111, 222222222]
    to: [1109489936]
  };
})(window);
